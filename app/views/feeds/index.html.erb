<h1 class="text-2xl font-bold mb-4"><%= t("posts.title") %></h1>

<div class="space-y-6">
  <% if @posts.empty? %>
    <p class="text-gray-600 mb-4"><%= t("posts.no_posts") %></p>
  <% else %>
    <%= render "filters" %>

    <% @posts.each do |post| %>
      <%= render "card", post: post %>
    <% end %>
  <% end %>
</div>
